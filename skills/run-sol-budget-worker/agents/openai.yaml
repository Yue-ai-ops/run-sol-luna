interface:
  display_name: "Run Sol + Budget Worker by Yue"
  short_description: "说“省额度”，严格使用 Luna Max 执行"
  default_prompt: "使用 $run-sol-budget-worker 完成当前目标：只接受 gpt-5.6-luna + max；当前已是 Luna Max 就直接执行，当前是 Sol 就继承上下文启动一个 Luna Max；不写固定任务卡、不重复普通工作，也不回退其他 Luna 档位或 Terra。"
policy:
  allow_implicit_invocation: true
