interface:
  display_name: "Run Sol + Budget Worker by Yue"
  short_description: "说“省额度”，优先 Luna Max；不可用时 Terra mid"
  default_prompt: "使用 $run-sol-budget-worker 省额度完成当前任务；默认 Sol high 负责规划和验收，优先 1 个 Luna Max 执行边界清晰的子任务；Luna 不可用时仅回退到 Terra medium，并明确标记回退。"
policy:
  allow_implicit_invocation: true
