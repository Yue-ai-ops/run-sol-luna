interface:
  display_name: "Run Sol + Luna Handoff by Yue"
  short_description: "说“省额度”：Luna Max 执行，支持交接与切回恢复"
  default_prompt: "使用 $run-sol-budget-worker 完成当前目标：优先由 gpt-5.6-luna + max 执行；模型切换先做最小交接与现场刷新；Sol 只在具体高风险问题上做顾问；不缩小用户目标、不重复普通工作、不降级 Luna 或自动回退 Terra。"
policy:
  allow_implicit_invocation: true
