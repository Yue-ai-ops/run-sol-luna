interface:
  display_name: "Run Sol + Budget Worker by Yue"
  short_description: "说“省额度”，Luna 主执行，Sol 只过关键关口"
  default_prompt: "使用 $run-sol-budget-worker 省额度完成当前任务：当前是 Luna Max 就直接执行；当前是 Sol 且可调用 Luna，就由 Sol 简短定向、Luna Max 执行，并按风险做最小必要核验；不要自动回退 Terra。"
policy:
  allow_implicit_invocation: true
