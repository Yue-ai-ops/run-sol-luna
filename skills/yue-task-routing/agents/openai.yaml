interface:
  display_name: "Yue Task Routing · 省额度"
  short_description: "按任务选模型与投入，保留目标和交接，避免多余编排与重复审查"
  default_prompt: "使用 $yue-task-routing 省额度完成当前目标：优先直接执行，只有独立任务或具体难题才委派或咨询，尊重我选择的模型。"
policy:
  allow_implicit_invocation: true
